{
  "vendor": true,
  "tasks": {
    "dev": "deno --allow-read --watch ./**/*.ts",
    "run-all": "deno --allow-read ./**/*.ts"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "fmt": {
    "singleQuote": true
  }
}
